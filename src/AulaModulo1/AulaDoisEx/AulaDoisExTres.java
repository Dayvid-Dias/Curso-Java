package AulaModulo1.AulaDoisEx;

public class AulaDoisExTres {
    //Declare uma variável do tipo char (letra) e uma variável do tipo String (palavra).
    // Atribua valores a essas variáveis e concatene-as em uma mensagem.
    public static void main(String[] args) {

       char letra = 'B';
       String palavra = "atata";

        System.out.printf("A letra é %c e a palavra é %s, juntando as duas resulta em %c%s!", letra, palavra, letra, palavra);

    }
}
